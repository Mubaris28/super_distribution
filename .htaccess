# Super Distribution - Apache config (Hostinger)
# Use for static HTML + optional PHP forms.
# URLs work without .html (e.g. /contact, /about). Upload to site root (public_html).

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Favicon: serve 32px PNG when browser requests /favicon.ico
  RewriteRule ^favicon\.ico$ assets/images/favicon/SD%20LOGO%2032px-01.png [L,NC]

  # Remove .html from browser: redirect /page.html -> /page (301)
  RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?] [NC]
  RewriteRule ^ /%1 [R=301,L,NE]

  # Serve .html when URL has no extension: /page or /products-innerpage/name -> .html
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME}.html -f
  RewriteRule ^(.+)$ $1.html [L]

  # Custom 404 (no file and no .html version)
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME}.html !-f
  RewriteRule ^(.*)$ 404.html [L,QSA]
</IfModule>

# Security: block direct access to config and logs
<FilesMatch "^(mail-config|security-config)\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
</FilesMatch>

# Disable directory listing
<IfModule mod_autoindex.c>
  Options -Indexes
</IfModule>

# Force HTTPS — uncomment after enabling SSL in Hostinger panel
# <IfModule mod_rewrite.c>
#   RewriteCond %{HTTPS} off
#   RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
# </IfModule>

# MIME types
<IfModule mod_mime.c>
  AddType application/json .json
  AddType image/svg+xml .svg
</IfModule>

# Caching for static assets
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpeg "access plus 1 month"
  ExpiresByType image/png "access plus 1 month"
  ExpiresByType image/webp "access plus 1 month"
  ExpiresByType image/svg+xml "access plus 1 month"
  ExpiresByType text/css "access plus 1 week"
  ExpiresByType application/javascript "access plus 1 week"
</IfModule>
